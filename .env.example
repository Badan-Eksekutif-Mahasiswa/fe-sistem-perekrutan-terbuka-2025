# Backend API Configuration
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Media Storage (R2) — upload handled via backend presigned URL
NEXT_PUBLIC_R2_PUBLIC_URL=https://pub-xxxx.r2.dev

# Environment
NODE_ENV=development

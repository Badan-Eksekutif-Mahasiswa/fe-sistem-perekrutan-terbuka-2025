# Backend API Configuration
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Media upload uses the backend /media/presign-upload endpoint and Cloudflare R2.
# Configure R2 variables in the backend environment.

# Environment
NODE_ENV=development
